# Backend runtime dependencies — every entry is imported at runtime OR
# is a transitive that pip may not resolve tightly enough without pinning.
# All installable from public PyPI on a clean Ubuntu 24.04 venv.

# --- Web framework ---
fastapi==0.110.1
uvicorn==0.25.0
python-multipart>=0.0.9        # required by fastapi.UploadFile/Form (admin restore endpoint)

# --- Data / validation ---
pydantic>=2.6.4

# --- HTTP client ---
httpx>=0.27.0
brotli>=1.1.0                   # httpx needs this to decode br-encoded responses (Skinport uses br)

# --- MongoDB ---
motor==3.3.1
pymongo>=4.5,<5                 # motor 3.3.1 pins pymongo>=4.5,<5 — explicit for reproducibility

# --- Auth / crypto ---
pyjwt>=2.10.1
bcrypt==4.1.3

# --- Config ---
python-dotenv>=1.0.1

# --- Payments ---
stripe==14.4.1

# --- Timezone data (needed by zoneinfo on some minimal Ubuntu installs) ---
tzdata>=2024.2
