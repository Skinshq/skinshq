# Dev / lint / test tooling — install only in dev with:
#   pip install -r requirements-dev.txt
# Never needed in production.
-r requirements.txt

black>=24.1.1
flake8>=7.0.0
isort>=5.13.2
mypy>=1.8.0
pytest>=8.0.0
pytest-xdist>=3.6.0
